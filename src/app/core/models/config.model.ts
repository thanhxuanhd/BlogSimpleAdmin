import { TableConfig, LanguageConfiguration } from './page.model';

export class Configuration {
    BASE_API: string;
    API_VERSION: string;
    PAGE_CONFIG: TableConfig;
    LAGUAGE: LanguageConfiguration;
}
